# 月度摸底考试 · 改造方案 v2

> 版本：v2.0（基于 v1.0 修订）
> 起草日期：2026-06-28
> 状态：**Phase 0 + Phase 1 + Phase 2 已落地**（截至 2026-06-28），Phase 3 进入规划阶段
> 与 v1 的关系：v1 文档保留作为历史参考，v2 是当前实际执行的版本

---

## 0. v2 相对 v1 的主要变化

调研发现原项目 README 截图中的多数功能（专项刷题/排行榜/激活码/我的等）**并未开源**——那是原作者的商业定制版本展示，开源代码只是骨架。同期对国内主流开源考试系统做了横向调研，结论是：

- 学之思 xzs（3587★，最成熟）需 Java + MySQL 后端，必须自建服务器+备案，不符合"零额外成本"约束。
- kesixin 系列（1584★ + 373★）实际是商业产品的引流仓库，不含真代码。
- 其它候选要么停更，要么体量太小。

最终决策：**继续在 `exam-mini-master` 骨架上改造**，并补齐 v1 中砍掉的关键能力——**HR Web 管理后台**。改造完整覆盖 v1 的全部目标 + v1「后续二期」的 Web 后台 + 首页 UI 美化。

### 0.1 与 v1 的差异点

| 项 | v1 | v2 |
|---|---|---|
| HR 管理后台 | 第一期用云开发控制台，二期再做 | **第一期就做**（基于云开发静态网站托管，零月费） |
| 首页 UI | 维持骨架现状 | **重做**：仿 README 宣传图的视觉风格（统计条 + 入口图标 + 通知卡片） |
| 项目脏页面 | 不动 | **清理** `userConsole / storageConsole / im` 等 30+ 个云开发 demo 页 |
| HR 后台登录 | 不涉及 | **小程序扫码取码 + 浏览器输入登录** |
| HR 后台技术栈 | 不涉及 | **风格 A**：单 HTML 文件 + Element Plus CDN + Vanilla JS（无构建工具，无 Node 依赖） |
| 总工作量 | ~5.6 天 | **~8.5~9.5 天** |

### 0.2 不变的部分

- 数据模型（4 张表的 schema）
- 5 个核心云函数职责（whoAmI / getServerTime / enterExam / saveDraft / submitExam）
- 防作弊设计
- 服务端判分、题目快照固化、随机抽题策略
- 总体业务流程

---

## 1. 需求背景

（沿用 v1 第 1 章，无变化）

在现有"微信小程序考试系统"基础上，**新增"月度摸底考试"能力**，用于企业内部对员工进行每月一次的统一培训摸底。

- 30 人左右规模，云开发免费档绰绰有余。
- 现有"模考"作为平时练习保留不动。
- 每月由 HR 发起一场全员限时摸底考试，设开考时间 + 答题时长。
- 员工**线下被通知**到考试时间，无需走微信订阅消息推送。
- 员工到点打开小程序 → 首页能看到考试通知 → 候考倒计时归零开考 → 答题 → 自动批改 → 保存成绩。
- 小程序**不对外开放**，仅限白名单内员工使用。
- HR 在 PC 浏览器使用 Web 后台日常管理。

---

## 2. 现状盘点

（沿用 v1 第 2 章，已无新增）

### 2.1 当前已完成的工作

| 工作 | 状态 |
|---|---|
| Git 初始化（用户 limiaoyan / 邮箱 lssens@163.com / 本地） | ✅ |
| 云开发环境切换为 `cloud1-d5gievact76bc75a4` | ✅ |
| 部署 `initDB` 云函数 | ✅ |
| 创建 6 个核心集合 + 种子数据（exam/subjects/questions/historys/notes/profiles） | ✅ |
| 真机验证答题流程跑通 | ✅ |
| 调研发现原 README 截图 ≠ 真实代码 | ✅ |
| 调研发现学之思因服务器成本不可选 | ✅ |

### 2.2 Phase 落地进度（截至 2026-06-28）

| Phase | 内容 | 状态 | 里程碑 |
|---|---|---|---|
| **Phase 0** | 清理 demo 页面 + TabBar 3 项 + 首页 UI 重做 + 全局标题改名 + 占位页 | ✅ 已完成 | tag `v0.1-phase1` |
| **Phase 1** | 员工白名单 + `whoAmI` / `activate` 云函数 + 激活页 + app.js 路由守卫 | ✅ 已完成 | tag `v0.1-phase1` |
| **Phase 2** | 考试服务端化：`getServerTime` / `enterExam` / `saveDraft` / `submitExam` / `listMyAssessments` 云函数 + 考试页防作弊 + 复盘 + 错题本 | ✅ 已完成 | tag `v0.2-phase2` |
| **Phase 3** | HR Web 管理后台（单 HTML + Element Plus CDN + 云开发静态托管） | ⬜ 待开工 | tag `v0.3-phase3`（计划） |
| **Phase 4** | 首页考试卡 4 态接真实数据 + `pages/waiting` 候考页（已部分提前落地于 Phase 2） | ⬜ 待开工 | tag `v0.4-phase4`（计划） |
| **Phase 5** | SOP 文档 + 抗作弊用例联调测试 + 正式上线 | ⬜ 待开工 | tag `v1.0-release`（计划） |

#### Phase 0 + Phase 1 实际产出

- **页面**：清理 11 个 demo 页 → 净保留 18 个业务页 + 1 个新激活页 = 19 个页
- **云函数**：新增 `whoAmI` + `activate` 共 2 个（旧 demo 云函数 callback/echo/openapi/sum 暂留不动）
- **数据库**：新增 `employees` 集合（含 3 条种子记录）
- **文档**：新增 `docs/数据库设计.md` + `docs/初始员工数据.json` + 2 份操作步骤文档

身份链路完整跑通 4 个分支：active / unactivated / disabled / error。

#### Phase 2 实际产出

- **云函数**：新增 5 个 — `getServerTime` / `enterExam` / `saveDraft` / `submitExam` / `listMyAssessments`
- **集合**：新增 `assessments`（HR 派发的考试）+ `examEnrollments`（每人每场答卷快照）
- **考试页**：`pages/exam` 完整重写 — 服务端取题 + 服务端时间戳倒计时 + 30s 自动暂存 + onHide 切屏计数 + 自动交卷
- **结果页**：`pages/examresult` 重写 — 顶部分数大图 + 三栏统计 + 模考可展开正确答案 + 正式考卡片引导到"我的成绩"复盘
- **复盘页**：`pages/review` 重写 — 题干 / 用户答案 / 标准答案 / 解析全展示，绿/红/虚框三色配色直观标识对错
- **错题本**：**新增** `pages/mistakes` — 聚合最近 20 次考试的错题，点击直达对应题目复盘
- **首页接入**：考试通知卡 + "错题本"入口 + "考试安排"页全部接 `listMyAssessments` 真实数据
- **跨页传参**：用 `app.globalData.lastExamResult` 传题目快照避免 URL 长度限制
- **关键修复**：云函数 `db.add` 不会自动注入 `_openid`，submitExam 写 historys 时显式带上，确保客户端能查到

防作弊四件套全部生效：服务端时间戳、答案不下发、抽题随机化、enrollment 防重入。

---

## 3. 总体方案

### 3.1 关键设计决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| 练习 vs 摸底如何区分 | `exam` 集合加 `mode` 字段（`free` / `assessment`） | 老题库零影响 |
| 员工身份 | **白名单 + openid 手动绑定**（一期）<br>**HR 后台一键绑定**（二期 v2 新增） | 不外开放，HR 工作量最小 |
| 题目来源 | **从大题库随机抽 N 题** | 降低对答案概率 |
| 抽到的题 | **进考场时固化到 enrollment 快照** | 续考一致、防重抽刷分 |
| 续考策略 | **允许续考，恢复草稿** | 体验最佳 |
| 时间基准 | **服务端时间，前端只显示** | 防止改本地系统时间作弊 |
| 判分位置 | **服务端云函数判分** | 防止前端篡改分数 |
| 答案下发 | **前端只拿题目，不拿 value 标记** | 防止扒接口拿答案 |
| 管理后台 | **第一期就做 Web 后台**（v2 修订） | 30 人规模长期使用，云开发控制台对 HR 不友好 |
| 后台技术栈 | **单 HTML + Element Plus CDN**（风格 A） | 无构建、无 Node、无服务器；上传单文件即可 |
| 后台部署 | **云开发静态网站托管** | 免费档 1GB 空间 + 5GB 流量/月，对 30 人足够 |
| HR 登录方式 | **小程序扫码取码 + 浏览器输入登录** | 安全（确认人是员工本人），简单（无需密码管理） |

### 3.2 整体架构

```
┌─────────────────────────────────────────────────┐
│  员工端（30 人）                                  │
│  ─────────────────                              │
│  微信小程序 exam-mini-master（改造）              │
│   ├─ 首页：通知卡片 + 入口                       │
│   ├─ 候考页 → 考试页 → 成绩页                    │
│   └─ 未激活时跳激活提示页                        │
└──────────────┬──────────────────────────────────┘
               │
               │ wx.cloud.callFunction
               │
               ▼
┌─────────────────────────────────────────────────┐
│  云开发环境 cloud1-d5gievact76bc75a4              │
│  ─────────────────                              │
│  ├─ 云函数（员工端 + HR 端共用）                  │
│  │   whoAmI / getServerTime / enterExam /       │
│  │   saveDraft / submitExam / adminLogin /      │
│  │   adminVerify / adminAPI                     │
│  │                                              │
│  ├─ 云数据库（employees / exam / questions /     │
│  │   subjects / examEnrollments / historys /    │
│  │   notes / profiles / adminSessions）         │
│  │                                              │
│  └─ 静态网站托管                                 │
│      └─ HR 后台 SPA（单 HTML 文件）              │
└──────────────▲──────────────────────────────────┘
               │
               │ fetch（带 token）
               │
┌──────────────┴──────────────────────────────────┐
│  HR 端                                          │
│  ─────────────────                              │
│  浏览器打开 https://xxx-xxx.tcloudbaseapp.com   │
│   ├─ 登录页：输入工号 + 小程序生成的 6 位码      │
│   ├─ 员工管理（白名单 + 绑定）                   │
│   ├─ 题库管理 + Excel 批量导入                  │
│   ├─ 考试管理（创建/发布/查看）                  │
│   └─ 成绩查询 + 导出                            │
└─────────────────────────────────────────────────┘
```

### 3.3 总体流程

```
HR 月初：
  1. 浏览器打开 HR 后台 → 用小程序扫码登录
  2. （首次）维护员工白名单 / 增量补充
  3. 处理员工 openid 激活请求（一键审核通过）
  4. 维护题库（手动添加 or Excel 导入）
  5. 创建本月考试（设时间、时长、抽题数）
  6. 发布考试 → 员工小程序首页自动出现通知卡片

员工：
  打开小程序
    └─ whoAmI 校验身份
        ├─ 未绑定 → 跳激活页（显示 openid，提示发给 HR）
        └─ 已绑定 → 进首页
            └─ 首页通知卡片显示本月考试状态
                ├─ 未开考 → 点卡片进候考页倒计时
                ├─ 进行中 → 候考页"开始考试"亮起
                │   └─ enterExam 抽题固化 → 考试页
                │       └─ 倒计时 + saveDraft → 交卷
                │           └─ submitExam 判分 → 成绩页
                └─ 已交卷 → 成绩页

HR 月末：
  1. 后台 → 考试管理 → 选本月考试 → 查看成绩
  2. 点"导出 Excel"下载
```

---

## 4. 数据模型

### 4.1 沿用 v1 的 4 张核心表

无任何变化，参考 v1 文档第 4 章。即：

- `employees`（新增）
- `exam`（扩展 mode/时间窗/抽题策略字段）
- `questions`（扩展 pool 字段）
- `examEnrollments`（新增）

### 4.2 v2 新增表

#### 4.2.1 `employees` 集合 · 角色字段补充

在 v1 的 schema 基础上补一个 `role` 字段：

```js
{
  _id: "E0023",
  name: "张三",
  phone: "13800001234",
  department: "研发部",
  openid: "",
  status: "active",
  bindTime: null,
  createTime: 1719500000000,

  // v2 新增
  role: "employee"          // "employee" | "admin"（HR 后台权限）
}
```

- 首批导入员工时全部为 `employee`
- HR 自己的记录需要手动改成 `admin`（在 initDB 或控制台改）

#### 4.2.2 `adminSessions` 集合（新增 · HR 后台会话）

```js
{
  _id: "随机生成的 token",          // 32 位随机串
  openid: "oXxxx",                  // HR 的 openid
  employeeId: "E0001",
  loginCode: "418293",              // 6 位数字，5 分钟有效
  codeExpire: 1719500700000,        // 取码过期时间
  status: "pending",                // pending（等浏览器输入）| active（已激活）| expired
  activatedAt: null,                // 激活时间（用 token 换登录态时记录）
  tokenExpire: 1719587400000,       // token 有效期（默认 24h）
  createTime: 1719500400000
}
```

**权限**：仅云函数读写，前端不可访问。

### 4.3 集合权限总览（v2）

| 集合 | 前端读 | 前端写 | 说明 |
|---|---|---|---|
| `employees` | ❌ | ❌ | 仅云函数 |
| `exam` | ✅（仅 visible=true） | ❌ | 首页展示考试通知 |
| `subjects` | ✅ | ❌ | 老练习模式用 |
| `questions` | ❌ | ❌ | 防止扒答案 |
| `historys` | ✅（仅自己） | ✅（仅自己） | 老练习模式 |
| `notes` | ✅（仅自己） | ✅（仅自己） | 老练习模式 |
| `profiles` | ✅（仅自己） | ✅（仅自己） | 老字段保留 |
| `examEnrollments` | ❌ | ❌ | 仅云函数 |
| **`adminSessions`** | ❌ | ❌ | 仅云函数 |

---

## 5. 云函数清单

v1 的 5 个 + v2 新增 3 个 = 共 8 个新云函数。

### 5.1 ~ 5.5 沿用 v1

`whoAmI / getServerTime / enterExam / saveDraft / submitExam` 见 v1 文档第 5 章。

### 5.2 v2 新增云函数

#### 5.6 `adminLogin` —— 小程序侧生成登录码

调用方：员工端微信小程序的"HR 后台入口"页

**入参**：无

**出参**：
```js
{
  ok: true,
  loginCode: "418293",        // 给 HR 看的 6 位数字
  expireIn: 300                // 5 分钟有效
}
```

**逻辑**：
1. `cloud.getWXContext().OPENID` 取 openid
2. 查 `employees` where openid 且 `role='admin'`，未命中 → `{ ok:false, msg:'无权限' }`
3. 生成 32 位随机 token + 6 位随机数字 code
4. 写 `adminSessions`：`status='pending'` `codeExpire=now+5*60*1000`
5. 返回 code（token 不返回前端，留在云数据库里待会换）

#### 5.7 `adminVerify` —— 浏览器侧用工号+码换 token

调用方：HR 后台 Web 登录页

**入参**：`{ employeeNo: "E0001", code: "418293" }`

**出参**：
```js
{
  ok: true,
  token: "32位随机串",
  expireIn: 86400,
  admin: { _id: "E0001", name: "李四", department: "HR" }
}
```

**逻辑**：
1. 查 `employees` 找到 `_id=employeeNo` 且 `role='admin'`
2. 查 `adminSessions` where `openid=employee.openid && loginCode=code && status='pending' && codeExpire>now`
3. 命中：更新 `status='active' activatedAt=now`
4. 返回 token（前端存 localStorage）

#### 5.8 `adminAPI` —— HR 后台所有业务请求的总入口

调用方：HR 后台所有操作

**入参**：`{ token, action, payload }`

**出参**：根据 action 不同

**支持的 action**（一期）：
- `listEmployees` / `createEmployee` / `updateEmployee` / `deleteEmployee` / `batchImportEmployees`
- `listPendingOpenids`（待绑定的员工 openid 申请）/ `bindOpenid`（一键绑定）
- `listQuestions` / `createQuestion` / `updateQuestion` / `deleteQuestion` / `batchImportQuestions`
- `listPools`（题池标签列表）
- `listExams` / `createExam` / `updateExam` / `publishExam` / `archiveExam`
- `listEnrollments`（成绩列表）/ `exportEnrollments`（导出 Excel 数据，由前端组装）
- `getStats`（首页仪表盘：员工数、题数、考试数、本月参考人数）

**统一鉴权**：所有 action 先用 token 查 `adminSessions` 验证有效性，再查 `employees` 验证 role=admin，最后才执行业务。

---

## 6. 页面改造

### 6.1 v1 已规划页面（不变）

- 新增 `pages/activate/activate` 未激活页
- 新增 `pages/waiting/waiting` 候考页
- 改造 `pages/home/index` 加通知卡片
- 改造 `pages/exam/exam` 加倒计时
- 改造 `app.onLaunch` 全局校验

### 6.2 v2 首页全面重做（关键变化）

参考用户期望的视觉效果，首页改为：

```
┌─────────────────────────────────┐
│  考试助手          🔍           │ ← 顶栏
├─────────────────────────────────┤
│  ┌─────────┬─────────────────┐  │
│  │ 已答题数 │  正确率          │  │ ← 统计条
│  │   128   │   85%           │  │
│  │ 排名 #3 │  排名 #5         │  │
│  └─────────┴─────────────────┘  │
├─────────────────────────────────┤
│  📢 2026年6月摸底考试            │ ← 考试通知卡片
│  6月27日 20:00 开考             │  （未开考/进行中/已交卷三态）
│  距开考 02:15:40    [进入候考]  │
├─────────────────────────────────┤
│  ┌─────┐  ┌─────┐  ┌─────┐    │ ← 入口图标 3x2 网格
│  │ 🎯  │  │ 📝  │  │ 📋  │    │
│  │顺序 │  │我的 │  │错题 │    │
│  │练习 │  │收藏 │  │本   │    │
│  └─────┘  └─────┘  └─────┘    │
│  ┌─────┐  ┌─────┐  ┌─────┐    │
│  │ 🎲  │  │ 📊  │  │ 👤  │    │
│  │随机 │  │我的 │  │个人 │    │
│  │刷题 │  │成绩 │  │信息 │    │
│  └─────┘  └─────┘  └─────┘    │
├─────────────────────────────────┤
│ 🏠首页    📊成绩    👤我的       │ ← TabBar 3 个
└─────────────────────────────────┘
```

入口对应关系（不做"专项刷题/题型刷题"等不存在的功能，只做真实有代码的功能）：

| 入口 | 跳转 | 说明 |
|---|---|---|
| 顺序练习 | 老 `pages/subject` | 选学科 → 选试卷 → 答题 |
| 随机刷题 | 老 `pages/simple` 改造 | 从练习题库随机抽 |
| 我的收藏 | 老 `pages/note` | 已有 |
| 错题本 | `pages/note` 加错题筛选 | 复用已有数据 |
| 我的成绩 | 老 `pages/history` 改造 | 已有 |
| 个人信息 | 新 `pages/profile`（重写老 info 页） | v1 的 info 页是死链，丢弃重写 |

### 6.3 v2 新增 HR 后台入口

在"个人信息"页底部，仅 role=admin 用户可见的入口：

```
[管理后台]  →  跳 pages/adminEntry
```

`pages/adminEntry`：
- 调 `adminLogin` 获取 6 位码
- 大字显示 6 位数字
- 显示后台访问地址 https://xxx-xxx.tcloudbaseapp.com
- "刷新取码"按钮

### 6.4 TabBar 简化

从骨架的 2 个 tab 改为 3 个：

```json
"tabBar": {
  "list": [
    {"text":"首页","pagePath":"pages/home/index", ...},
    {"text":"我的成绩","pagePath":"pages/history/index", ...},
    {"text":"我的","pagePath":"pages/profile/index", ...}
  ]
}
```

---

## 7. HR Web 管理后台设计（v2 新增章节）

### 7.1 技术栈（风格 A）

- 单 HTML 文件，名为 `index.html`
- CDN 引入：Vue 3 + Element Plus + Axios
- 不需要 npm，不需要 webpack/vite，**直接用记事本就能改**
- 部署：把 `index.html` 通过云开发控制台上传到"静态网站托管"

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>考试管理后台</title>
  <link rel="stylesheet" href="https://unpkg.com/element-plus/dist/index.css">
  <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
  <script src="https://unpkg.com/element-plus/dist/index.full.js"></script>
  <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
</head>
<body>
  <div id="app">...</div>
  <script>
    // 全部业务代码在这里，单文件
  </script>
</body>
</html>
```

### 7.2 后台调云函数的桥接

云开发的云函数本来只能从小程序调，浏览器调需要通过 **HTTP 触发器**（云开发提供的标准能力）。`adminAPI` 云函数同时部署为 HTTP 触发器，得到一个 URL：

```
https://xxx-xxx.service.tcloudbase.com/adminAPI
```

浏览器用 `axios.post` 调这个地址，请求体带 `{ token, action, payload }`。

### 7.3 页面清单

| 页面 | 功能 |
|---|---|
| 登录页 | 输入工号 + 小程序生成的 6 位码，调 adminVerify |
| 仪表盘 | 员工数、题库数、本月考试参考人数、最近 5 条提交 |
| 员工管理 | 员工列表（搜索/筛选）/ 新建 / 编辑 / 禁用 / Excel 批量导入 / openid 绑定 |
| 题库管理 | 题目列表（按 pool 筛选）/ 新建 / 编辑 / 删除 / Excel 批量导入 |
| 题池管理 | 列出所有 pool 标签，统计每个 pool 下题数 |
| 考试管理 | 考试列表 / 新建（设时间、时长、抽题数、关联 pool）/ 发布 / 归档 |
| 成绩查询 | 按考试筛选 / 按部门筛选 / 导出 Excel（前端组装 CSV 下载） |

### 7.4 HR 登录流程图

```
[1] HR 浏览器打开 https://xxx-xxx.tcloudbaseapp.com
    └─ 显示登录页：工号 + 登录码 输入框

[2] HR 拿出手机：
    打开小程序 → 我的 → 管理后台 → 屏幕显示"418293"
    （内部：小程序调 adminLogin，云函数返回 code，写 adminSessions）

[3] HR 在浏览器输入工号 E0001 + 码 418293 → 点登录
    └─ 浏览器调 adminVerify
        └─ 云函数验证 employees(E0001).role=admin
        └─ 云函数验证 adminSessions 里有匹配的 pending 记录
        └─ 通过则返回 token，session 标 active

[4] 浏览器存 token 到 localStorage，跳仪表盘
    后续所有请求 header 带 token
```

### 7.5 HR 后台部署 SOP

仅需一次：

1. 进微信开发者工具 → 云开发 → 静态网站托管 → 开通（免费）
2. 拿到分配的域名 `xxx-xxx.tcloudbaseapp.com`
3. 用控制台的"文件上传"功能传 `index.html`
4. 即时生效，HR 浏览器打开即用

---

## 8. 防作弊设计

（沿用 v1 第 7 章 + 后台鉴权补充）

| 攻击方式 | 防护 |
|---|---|
| 改手机系统时间提前进场 | 服务端时间判断 |
| 改手机系统时间不交卷 | 服务端时间判超时 |
| 同人换设备刷分 | enrollment 主键 = examId + openid |
| 答完反悔重做 | enterExam 看到 status=submitted 拒绝 |
| 改前端代码篡改分数 | 服务端判分 |
| 扒接口拿正确答案 | 前端只下发题目，不下发 value |
| 直接读题库 | questions 集合前端不可读 |
| 直接改 enrollment 分数 | examEnrollments 集合前端不可写 |
| **后台被陌生人访问**（v2 新增） | **每次请求验证 token + role=admin** |
| **token 泄露**（v2 新增） | **24 小时自动过期 + 后台可主动注销** |
| **猜 6 位码暴力破解**（v2 新增） | **5 分钟过期 + 每个工号同时仅一个 pending** |

---

## 9. HR 月度操作 SOP（v2 重写）

### 9.1 首次部署（一次性）

由开发者操作：

1. 在 `employees` 集合手动添加 HR 自己一条记录，`role='admin'`，绑好 openid
2. 在云开发控制台开通"静态网站托管"
3. 上传 HR 后台 `index.html`
4. 告知 HR 后台地址 + HR 自己的工号

之后由 HR 操作：

1. HR 用小程序登录 → 进 HR 后台
2. 后台 → 员工管理 → "导入 Excel"
3. 后台 → 题库管理 → 创建题池 / 导入题目 Excel
4. 测试创建一场预演考试验证流程

### 9.2 日常操作

| 任务 | 频率 | 操作 |
|---|---|---|
| 新员工入职 | 不定期 | 后台 → 员工管理 → 新建（openid 留空） |
| 员工激活账号 | 不定期 | 员工把 openid 截图发 HR → 后台 → 待绑定列表 → 一键绑定 |
| 出本月题目 | 月初 | 后台 → 题库管理 → 维护 pool=monthly-pool-2026-07 标签下题目 |
| 创建本月考试 | 月初 | 后台 → 考试管理 → 新建 → 选 pool → 设时间 → 发布 |
| 查看成绩 | 月末 | 后台 → 成绩查询 → 选本月考试 → 导出 Excel |
| 员工离职 | 不定期 | 后台 → 员工管理 → 改 status=leaved |

---

## 10. 工作量评估（v2）

| 阶段 | 模块 | 工时 |
|---|---|---|
| **Phase 0**<br>项目清理 | 删 `app.json` 中的云开发 demo 页（30+ 个）<br>删对应 wxml/js/wxss 文件夹<br>TabBar 改为 3 项<br>添加 `pages/profile` 骨架 | 0.25 天 |
| | 首页 UI 重做：统计条 + 图标网格 + 通知卡片 | 0.75 天 |
| **Phase 1**<br>身份与白名单 | 数据模型固化 + 集合权限 + employees 加 role 字段 | 0.25 天 |
| | `whoAmI` 云函数 | 0.25 天 |
| | `pages/activate` 未激活页 | 0.25 天 |
| | `app.onLaunch` 全局校验 | 0.25 天 |
| | 准备测试数据（含 1 HR + 5 员工） | 0.25 天 |
| **Phase 2**<br>考场核心 | `getServerTime` 云函数 | 0.1 天 |
| | `enterExam` 云函数（含抽题、打乱、快照） | 0.75 天 |
| | `saveDraft` 云函数 | 0.25 天 |
| | `submitExam` 云函数（含服务端判分） | 0.5 天 |
| | `pages/exam` 改造（倒计时 + 草稿恢复 + 自动交卷） | 0.75 天 |
| **Phase 3**<br>通知与候考 | 首页通知卡片 4 种状态联动 | 0.25 天 |
| | `pages/waiting` 候考页 | 0.5 天 |
| | 时间窗边界测试 | 0.25 天 |
| **Phase 4**<br>HR 后台 | `adminLogin` / `adminVerify` 云函数 | 0.25 天 |
| | `adminAPI` 云函数（含所有 action 路由 + 鉴权） | 1 天 |
| | HTTP 触发器配置 | 0.1 天 |
| | `pages/adminEntry` 小程序侧入口 | 0.25 天 |
| | HR 后台 `index.html`：登录页 + 框架 + 路由 | 0.5 天 |
| | HR 后台：员工管理（列表/CRUD/Excel 导入/绑定） | 0.75 天 |
| | HR 后台：题库管理（列表/CRUD/Excel 导入） | 0.75 天 |
| | HR 后台：考试管理 + 成绩查询 + 导出 Excel | 0.5 天 |
| | 静态网站托管部署 | 0.1 天 |
| **Phase 5**<br>联调上线 | HR SOP 文档（含截图） | 0.25 天 |
| | 防作弊场景测试 | 0.5 天 |
| | 全流程演练 | 0.25 天 |
| **合计** | | **~9.25 天** |

---

## 11. 实施计划

按依赖顺序分 6 个阶段，每阶段可独立验证。

### Phase 0 · 项目清理 + 首页 UI（1 天）

里程碑：员工打开小程序看到的首页 UI 接近商业产品水准。

- [ ] 列出所有要删的 demo 页（先确认清单）
- [ ] 删 `app.json` 里相应页面 + 对应文件夹
- [ ] TabBar 改 3 项
- [ ] 设计首页新布局并实现
- [ ] 加 `pages/profile` 占位页

### Phase 1 · 身份与白名单（1.25 天）

里程碑：员工能激活账号，HR 能在数据库看到激活请求。

- [ ] 落 v2 schema 到 `docs/schema.md`
- [ ] 控制台建 `employees / examEnrollments / adminSessions` 集合 + 权限
- [ ] `questions` 表加 `pool` 字段
- [ ] 写 `whoAmI`
- [ ] 写 `pages/activate`
- [ ] 改 `app.onLaunch`
- [ ] 准备 1 个 HR + 5 个员工测试数据

### Phase 2 · 考场核心（2.35 天）

里程碑：员工能进考场答题交卷看分。

- [ ] 4 个云函数
- [ ] `pages/exam` 改造
- [ ] 完整流程测试

### Phase 3 · 通知与候考（1 天）

里程碑：员工首页能看到考试通知，能候考。

- [ ] 首页通知卡片 4 态
- [ ] 候考页
- [ ] 边界测试

### Phase 4 · HR 后台（4.2 天）⭐ 工作量最大

里程碑：HR 能在浏览器完成所有月度操作，不再依赖云开发控制台。

- [ ] `adminLogin / adminVerify / adminAPI` 三个云函数
- [ ] 配置 `adminAPI` HTTP 触发器
- [ ] 小程序侧 `pages/adminEntry`
- [ ] HR 后台 `index.html` 基础框架
- [ ] HR 后台员工管理模块
- [ ] HR 后台题库管理模块
- [ ] HR 后台考试管理 + 成绩模块
- [ ] 静态网站托管部署

### Phase 5 · 联调与文档（1 天）

里程碑：HR 看完文档能独立完成全流程。

- [ ] HR SOP 文档（含截图）
- [ ] Excel 模板（员工 + 题目）
- [ ] 防作弊 + 边界测试
- [ ] 走一遍真实链路

---

## 12. 后续可做（不在本次范围）

- 微信订阅消息（让员工到点收推送）
- 成绩统计与分析（按部门、按时间趋势）
- 错题集中复习模式
- 多场考试并行
- HR 后台手机端响应式适配
- 多 HR 角色权限分级

---

## 13. 待确认事项

继承 v1 的 + 新增的：

- [ ] HR 后台是否需要 "导出 PDF 成绩单"？（一期只导 Excel）
- [ ] 多选题打分策略：完全一致才得分 / 漏选给一半 / 自定义？
- [ ] 成绩页是否对员工显示正确答案 / 解析？
- [ ] HR 是否需要"撤回考试" "作废某员工成绩"？
- [ ] HR 后台是否需要"操作日志"审计？

---

## 14. 变更记录

| 日期 | 版本 | 修改 |
|---|---|---|
| 2026-06-27 | v1.0 | 初稿 |
| 2026-06-28 | v2.0 | 新增 HR Web 后台（第一期就做）；新增首页 UI 重做；新增项目清理 Phase 0；HR 登录方式选定为小程序扫码取码；技术栈定为单 HTML+CDN（风格 A）；总工作量从 5.6 天增至 ~9.25 天 |
