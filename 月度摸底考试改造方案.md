# 月度摸底考试 · 改造方案

> 版本：v1.0（待评审）
> 起草日期：2026-06-27
> 状态：方案已确认，待开干

---

## 1. 需求背景

在现有"微信小程序考试系统"基础上，**新增"月度摸底考试"能力**，用于企业内部对员工进行每月一次的统一培训摸底。

### 1.1 业务诉求

- 现有的"模考"作为**平时练习**保留不动，员工任何时间可随到随练，不计入考核。
- 每月由 HR 发起一场**全员限时摸底考试**：
  - HR 设定开考时间（例如本月最后一周三 20:00）和答题时长（例如 30 分钟）。
  - 员工**线下被通知**到考试时间，无需走微信订阅消息推送。
  - 员工到点打开小程序 → 首页能看到考试通知 → 进入候考 → 倒计时归零开考 → 答题 → 自动批改 → 保存成绩。
- 小程序**不对外开放**，仅限白名单内员工使用。

### 1.2 范围与边界

| 范围内 | 范围外 |
|---|---|
| 限时摸底考试创建（HR 在云控制台操作） | 微信订阅消息推送 |
| 员工身份白名单 + openid 绑定 | 公众号模板消息 |
| 随机抽题、选项打乱 | 视频/语音类题型 |
| 进考场倒计时、自动交卷 | 人脸识别、防切屏告警 |
| 服务端判分、防作弊 | 全功能管理后台（二期再做） |
| 续考 / 草稿恢复 | 多场考试并行 |

### 1.3 不做哪些事（明确放弃）

- ❌ 不做微信订阅消息 / 公众号推送 —— 改用线下通知。
- ❌ 不做小程序内管理员页面 —— 第一期 HR 直接用云开发控制台维护数据。
- ❌ 不做硬防作弊（截屏、切屏、双机对答案等）—— 月度内训场景容忍度足够。
- ❌ 不替换现有练习模式 —— 两套并存。

---

## 2. 现状盘点

### 2.1 现有功能（可复用）

| 能力 | 位置 | 状态 |
|---|---|---|
| 单题模式答题 | `pages/exam/exam.js` | ✅ 完整 |
| 列表模式答题 | `pages/simple/index.js` | ✅ 完整 |
| 前端自动判分 | `pages/exam/exam.js` `pages/simple/index.js` | ✅ 完整（**摸底模式需迁移到云函数**） |
| 答题历史 | `pages/history` + `historys` 集合 | ✅ 完整 |
| 错题本 | `pages/note` + `notes` 集合 | ✅ 完整 |
| 成绩页 | `pages/score` `pages/result` `pages/examresult` | ✅ 完整 |
| openid 获取 | `cloudfunctions/login` | ✅ 完整 |
| 用户档案集合 | `data/profiles.json` | ⚠️ 集合已预留，逻辑未完整使用 |

### 2.2 现有缺口

- ❌ `exam` 集合仅有 `{_id, name}`，无任何时间字段。
- ❌ 全代码搜过 `setInterval / countdown / 倒计时 / 定时 / startTime / endTime`，**业务页面零定时逻辑**。
- ❌ 无员工身份机制，仅按 openid 识别。
- ❌ 无白名单准入，任何用户都能进入小程序。
- ❌ 判分在前端，正确答案随题目下发，**摸底场景不可用**。
- ❌ 无防重复参加机制，理论上可无限重做。

---

## 3. 总体方案

### 3.1 关键设计决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| 练习 vs 摸底如何区分 | `exam` 集合加 `mode` 字段（`free` / `assessment`） | 老题库零影响 |
| 员工身份 | **白名单 + openid 手动绑定** | 不外开放，HR 工作量最小，无需企业微信 |
| 题目来源 | **从大题库随机抽 N 题** | 降低对答案概率，HR 维护成本低 |
| 抽到的题 | **进考场时固化到 enrollment 快照** | 续考一致、复核可追溯、防重抽刷分 |
| 续考策略 | **允许续考，恢复草稿** | 体验最佳，deadline 在服务端固定不可作弊 |
| 时间基准 | **服务端时间，前端只显示** | 防止改本地系统时间作弊 |
| 判分位置 | **服务端云函数判分** | 防止前端篡改分数 |
| 答案下发 | **前端只拿题目，不拿 value 标记** | 防止扒接口拿答案 |
| 管理后台 | **第一期用云开发控制台** | 砍掉 1~2 天，二期再做 Web 后台 |

### 3.2 总体流程

```
HR 月初：
  1. 维护 employees 白名单（首次全量，后续增量）
  2. 处理员工发来的 openid，粘贴到 employees 对应记录
  3. 在 exam 集合创建本月考试（设时间、时长、抽题数）
  4. 维护 questions 题库（打 pool 标签）

员工：
  打开小程序
    └─ whoAmI 校验身份
        ├─ 未绑定 → 跳 pages/activate（显示 openid，提示发给 HR）
        └─ 已绑定 → 进 pages/home
            └─ 查询本月摸底考试 → 显示通知卡片
                ├─ 未开考 → 候考页倒计时
                ├─ 进行中 → 候考页"开始考试"按钮亮起
                │   └─ 点击 → enterExam 抽题固化 → 进 pages/exam
                │       └─ 倒计时 + saveDraft → 主动/自动交卷
                │           └─ submitExam 服务端判分 → 成绩页
                └─ 已交卷 → 成绩页
```

---

## 4. 数据模型

### 4.1 `employees` 集合（新增 · 员工白名单）

```js
{
  _id: "E0023",            // 工号做主键
  name: "张三",
  phone: "13800001234",    // 选填
  department: "研发部",     // 选填
  openid: "",              // 初始空，HR 后续绑定
  status: "active",        // active | leaved
  bindTime: null,          // openid 绑定时间
  createTime: 1719500000000
}
```

**索引建议**：在 `openid` 上建唯一索引（云开发数据库支持索引）。

**权限**：仅允许云函数读写，前端不可读不可写。

### 4.2 `exam` 集合（扩展）

老字段保留，新增以下字段。`mode='free'` 的老记录不带这些字段也能正常工作。

```js
{
  _id: "2026-06",
  name: "2026年6月摸底考试",

  // —— 模式分流 ——
  mode: "assessment",                // "free" 老练习 | "assessment" 摸底

  // —— 时间窗 ——
  startTime: 1719500400000,          // 开考时间（ms 时间戳）
  endTime:   1719504000000,          // 截止入场时间
  duration:  1800,                   // 单人答题时长（秒）

  // —— 抽题策略 ——
  questionPool: "monthly-pool",      // 题池标签
  questionCount: 30,
  shuffleQuestions: true,
  shuffleOptions: true,

  // —— 评分 ——
  scorePerQuestion: null,            // null=平均分；可传数组按题打分
  passScore: 60,

  // —— 其它 ——
  visible: true,
  remark: "全员参加，限时30分钟"
}
```

### 4.3 `questions` 集合（扩展 · 加 pool 标签）

老字段全部保留。新增 `pool` 字段用于摸底模式抽题。

```js
{
  _id: "...",
  title: "...",
  typecode: "01",
  typename: "单选",
  options: [{ code:"A", content:"...", value:"1" }, ...],
  comments: "题目解析",
  examid: "001001",                  // 老字段（练习模式用）保留
  pool: ["monthly-pool", "monthly-pool-2026-q3"]   // 新增，可属多池
}
```

**权限**：客户端不可读（防扒答案），所有读取走云函数。

### 4.4 `examEnrollments` 集合（新增 · 一人一场一条）

```js
{
  _id: "2026-06_oXxxAbCd",           // examId + openid，天然防重复
  examId: "2026-06",
  openid: "oXxxAbCd",

  // 冗余员工信息，便于 HR 报表
  employeeId: "E0023",
  name: "张三",
  department: "研发部",

  // 题目快照（进考场时固化，不含答案 value）
  questionSnapshot: [
    {
      _id: "q123",
      title: "...",
      typecode: "01",
      typename: "单选",
      options: [
        { code:"A", content:"..." },   // 注意：不含 value
        { code:"B", content:"..." }
      ]
    }
  ],

  // 时间
  enterTime:  1719500401000,
  deadline:   1719502201000,         // = enterTime + duration*1000
  submitTime: null,

  // 答题数据
  answers: { "q123": ["A"], "q456": ["B","C"] },
  lastDraftTime: 1719500500000,

  // 状态与成绩
  status: "ongoing",                 // ongoing | submitted | timeout
  score:    null,
  rightCount: null,
  totalCount: 30
}
```

**权限**：仅允许云函数读写。

### 4.5 集合权限总览

| 集合 | 前端读 | 前端写 | 说明 |
|---|---|---|---|
| `employees` | ❌ | ❌ | 仅云函数 |
| `exam` | ✅（仅 visible=true） | ❌ | 首页要展示考试通知 |
| `subjects` | ✅ | ❌ | 老练习模式用 |
| `questions` | ❌ | ❌ | 防止扒答案 |
| `historys` | ✅（仅自己） | ✅（仅自己） | 老练习模式用 |
| `notes` | ✅（仅自己） | ✅（仅自己） | 老练习模式用 |
| `profiles` | ✅（仅自己） | ✅（仅自己） | 老字段保留 |
| `examEnrollments` | ❌ | ❌ | 仅云函数 |

---

## 5. 云函数清单

新增 5 个云函数，原有 `login / echo / sum / callback / openapi` 不动。

### 5.1 `whoAmI`

**职责**：返回当前用户的 openid + 员工绑定情况。

**入参**：无

**出参**：
```js
{
  openid: "oXxx",
  isBound: true,
  employee: {
    _id: "E0023",
    name: "张三",
    department: "研发部"
  }  // 未绑定时为 null
}
```

**逻辑**：
1. `cloud.getWXContext().OPENID` 取 openid
2. 查 `employees` where `openid` = openid 且 `status='active'`
3. 命中则返回员工信息，否则 `isBound:false`

### 5.2 `getServerTime`

**职责**：返回服务端当前时间戳，给前端校时用。

**出参**：`{ now: 1719500400000 }`

### 5.3 `enterExam`

**职责**：员工进入考场，校验权限 + 创建/读取 enrollment + 首次进入时抽题。

**入参**：`{ examId: "2026-06" }`

**出参**：
```js
{
  ok: true,
  serverNow: 1719500401000,
  deadline:  1719502201000,
  questions: [...],          // 不含 value 字段
  draftAnswers: {...},       // 已存草稿
  status: "ongoing"
}
```

**核心逻辑**：
1. 校验员工身份（调 whoAmI 逻辑），未绑定直接拒绝
2. 查 `exam` 表，校验 `mode=assessment` `visible=true` `startTime ≤ now ≤ endTime`
3. 查 `examEnrollments._id = examId_openid`：
   - **不存在**：从 `questions` 池中按 pool 标签随机抽 `questionCount` 道题，乱序题目、乱序选项（如果配了），**剥离 value 字段**，写入 enrollment.questionSnapshot，设 `enterTime = now`、`deadline = now + duration*1000`，`status='ongoing'`
   - **存在且 status=ongoing**：直接返回快照 + 草稿
   - **存在且 status=submitted/timeout**：返回错误 "已交卷"
4. 校验 `now < deadline`，否则更新 status=timeout 并返回错误

### 5.4 `saveDraft`

**职责**：实时保存答题草稿。

**入参**：`{ examId, answers: {questionId: [...selectedCodes]} }`

**出参**：`{ ok: true, serverNow }`

**逻辑**：
1. 取 openid，查 enrollment
2. 校验 `status='ongoing'` 且 `now < deadline`，否则拒收
3. 更新 `answers` 和 `lastDraftTime`

**前端调用频率**：throttle 5 秒一次，且每次选项变更后触发。

### 5.5 `submitExam`

**职责**：交卷，服务端判分，落库。

**入参**：`{ examId }`

**出参**：
```js
{
  ok: true,
  score: 80,
  rightCount: 24,
  totalCount: 30,
  passed: true,
  details: [...]   // 可选：每题对错明细
}
```

**核心逻辑**：
1. 取 openid，查 enrollment，校验 status=ongoing
2. 取 `enrollment.questionSnapshot` 的所有 `_id`，回查 `questions` 表拿到真答案
3. 对每道题用 `enrollment.answers[qid]` 与真答案比对：
   - 单选：选中且对 → 得分
   - 多选：完全一致才得分（一期策略，简单清晰）
4. 计算总分（按 `exam.scorePerQuestion` 配置或平均分）
5. 写 `historys` 集合（沿用现有结构，加 `examId` 字段）
6. 更新 enrollment：`status='submitted'` `score` `rightCount` `submitTime`
7. 返回结果

**自动超时**：前端倒计时归零自动调此云函数；用户超时后才点交卷的，云函数检测 `now > deadline` 时按 timeout 处理（按当前 answers 评分）。

---

## 6. 页面改造

### 6.1 新增页面

#### `pages/activate/activate` —— 未激活提示页

```
┌─────────────────────────────┐
│      尚未激活账号             │
│                              │
│   您的小程序 ID：             │
│   ┌──────────────────────┐  │
│   │ oXxxAbCdEf123456789  │  │
│   └──────────────────────┘  │
│        [ 复制 ID ]           │
│                              │
│   请将此 ID 发送给 HR        │
│   完成身份绑定后重新打开     │
│                              │
│        [ 刷新检查 ]          │
└─────────────────────────────┘
```

- 调用 `wx.setClipboardData` 一键复制
- "刷新检查"按钮重新调 `whoAmI`，已绑定则跳首页

#### `pages/waiting/waiting` —— 候考页

```
┌─────────────────────────────┐
│      2026年6月摸底考试        │
│                              │
│       距开考还有              │
│      ┌──────────────┐        │
│      │   00:14:32   │        │
│      └──────────────┘        │
│                              │
│      答题时长：30 分钟       │
│      题目数量：30 题         │
│      备注：全员参加          │
│                              │
│      [ 开始考试 ] ← 灰/绿    │
└─────────────────────────────┘
```

- 进入时调 `getServerTime` 校时，后续本地 setInterval 自减
- 每 30 秒重新调一次 `getServerTime` 防止系统时间漂移
- 倒计时归零后按钮变绿，可点击
- 点击 → 调 `enterExam` → 跳 `pages/exam`

### 6.2 改造页面

#### `pages/home/index` —— 首页加考试通知卡片

```
┌─────────────────────────────┐
│  [通知卡片]                  │
│  📢 2026年6月摸底考试        │
│  6月27日 20:00 开考          │
│  距开考还有 02:15:40         │
│  ────────────────  →        │
│                              │
│  [原有的练习入口]             │
└─────────────────────────────┘
```

逻辑：
- `onShow` 时调 `whoAmI` + 查 `exam` (mode=assessment, visible=true, endTime>now)
- 根据状态显示：未开考 / 进行中 / 已交卷 / 已截止

#### `pages/exam/exam` —— 顶部加倒计时条 + 改造

```
┌─────────────────────────────┐
│ ⏰ 剩余 28:45    [交卷]      │ ← 新增固定顶栏
├─────────────────────────────┤
│  原有的题目区域              │
└─────────────────────────────┘
```

改造点：
- 顶部加倒计时条（红色字体，最后 5 分钟闪烁）
- `selectOption` 触发 throttle 调 `saveDraft`
- 倒计时归零自动调 `submitExam`
- **去掉前端判分**，改为提交后跳成绩页用云函数返回结果
- 进入时如果有草稿 `draftAnswers`，恢复选中状态
- 区分 mode：练习模式走老逻辑，摸底模式走新逻辑

### 6.3 app.onLaunch 全局身份校验

```js
// app.js
onLaunch: async function() {
  wx.cloud.init({ env: '...', traceUser: true })
  const res = await wx.cloud.callFunction({ name: 'whoAmI' })
  this.globalData.openid = res.result.openid
  this.globalData.employee = res.result.employee
  this.globalData.isBound = res.result.isBound

  if (!res.result.isBound) {
    wx.reLaunch({ url: '/pages/activate/activate' })
  }
}
```

---

## 7. 防作弊设计汇总

| 攻击方式 | 防护 | 位置 |
|---|---|---|
| 改手机系统时间提前进场 | 服务端时间判断 | `enterExam` |
| 改手机系统时间不交卷 | 服务端时间判超时 | `submitExam` |
| 同人换设备刷分 | enrollment 主键 = examId + openid | 数据库唯一性 |
| 答完反悔重做 | enterExam 看到 status=submitted 拒绝 | `enterExam` |
| 中途切后台、断网 | saveDraft 持续存草稿 | 体验保障 |
| 改前端代码篡改分数 | 服务端判分 | `submitExam` |
| 扒接口拿正确答案 | 前端只下发题目，不下发 value | `enterExam` |
| 进出多次刷新抽题 | 题目快照固化在 enrollment | `enterExam` |
| 直接读题库 | questions 集合前端不可读 | 数据库权限 |
| 直接改 enrollment 分数 | examEnrollments 集合前端不可写 | 数据库权限 |

> 以上为软防作弊。截屏给同事、双机对答案等硬作弊不在防护范围内。

---

## 8. HR 月度操作 SOP

> 第一期 HR 全程使用 **云开发控制台** 操作，不写代码。

### 8.1 首次部署（一次性）

1. 准备员工白名单 Excel（字段：工号、姓名、电话、部门）
2. 用批量导入工具导入 `employees` 集合（`openid` 字段留空）
3. 准备题库 Excel，标好 `pool` 字段
4. 用批量导入工具导入 `questions` 集合

### 8.2 处理员工激活（日常）

1. 员工发来"我的小程序 ID：oXxxxxxxx"
2. 进云开发控制台 → `employees` → 按姓名/工号查到此员工
3. 把 openid 粘贴到 `openid` 字段，保存
4. 通知员工"已激活，请重新打开小程序"

### 8.3 创建本月考试（每月一次）

1. 进云开发控制台 → `exam` 集合 → 新增记录
2. 复制上月模板，改以下字段：
   - `_id`：本月编号，如 `2026-07`
   - `name`：考试名称
   - `startTime`：开考时间戳（用 https://tool.lu/timestamp 转换）
   - `endTime`：截止入场时间戳
   - `duration`：答题时长（秒）
   - `questionPool`：本月用的题池标签
   - `questionCount`：抽题数量
   - `remark`：备注

### 8.4 考完导出成绩（每月一次）

1. 进云开发控制台 → `examEnrollments` → 按 `examId` 过滤
2. 导出 JSON，用脚本转 Excel（或后续做导出云函数）
3. 表头包含：工号、姓名、部门、得分、对题数、提交时间、状态

---

## 9. 工作量评估

| 模块 | 工时 |
|---|---|
| 数据模型固化（schema 文档 + 控制台建表 + 权限） | 0.25 天 |
| `whoAmI` 云函数 | 0.25 天 |
| `getServerTime` 云函数 | 0.1 天 |
| `enterExam` 云函数（含抽题、打乱、快照） | 0.75 天 |
| `saveDraft` 云函数 | 0.25 天 |
| `submitExam` 云函数（含服务端判分） | 0.5 天 |
| `pages/activate` 未激活页 | 0.25 天 |
| `pages/waiting` 候考页 | 0.5 天 |
| 首页通知卡片 | 0.25 天 |
| `pages/exam` 改造（倒计时 + saveDraft + 自动交卷 + 草稿恢复） | 0.75 天 |
| `app.onLaunch` 全局身份校验 + 路由拦截 | 0.25 天 |
| HR SOP 文档 + 员工白名单 Excel 模板 | 0.5 天 |
| 联调测试（含改时间作弊、断网续考、超时场景） | 1 天 |
| **合计** | **~5.6 天（一周）** |

---

## 10. 实施计划

按依赖顺序分四个阶段，每个阶段可独立验证：

### Phase 1 · 数据与身份（1 天）

里程碑：员工能激活账号，进入首页能看到自己的名字。

- [ ] 落地 4 张表的 schema 到 `docs/schema.md`
- [ ] 云控制台建好 `employees` / `examEnrollments` 集合及权限
- [ ] `questions` 表增加 `pool` 字段
- [ ] 写 `whoAmI` 云函数
- [ ] 写 `pages/activate` 页
- [ ] 改造 `app.onLaunch` 全局拦截
- [ ] 准备 5 条测试员工数据（含已绑定 / 未绑定两种状态）

### Phase 2 · 考场核心（2 天）

里程碑：员工能进考场、答题、交卷、看分。

- [ ] 写 `getServerTime` `enterExam` `saveDraft` `submitExam` 四个云函数
- [ ] 改造 `pages/exam`：倒计时条 + saveDraft + 自动交卷 + 草稿恢复 + 去前端判分
- [ ] 测试完整考试流程

### Phase 3 · 通知与候考（1 天）

里程碑：员工打开小程序能看到考试通知，能正常候考。

- [ ] 首页加考试通知卡片（4 种状态）
- [ ] 写 `pages/waiting` 候考页
- [ ] 测试时间窗的边界（开考前 1 秒、开考后 1 秒、截止前 1 秒）

### Phase 4 · 文档与联调（1.5 天）

里程碑：HR 能独立完成一次完整月度考试操作。

- [ ] HR SOP 文档（含截图）
- [ ] 员工白名单 Excel 模板
- [ ] 考试创建模板（JSON 片段）
- [ ] 防作弊场景测试：
  - 改系统时间提前进场
  - 改系统时间不交卷
  - 同人多次进入是否抽到同一套题
  - 答完后再进是否被拒
  - 断网 30 秒后是否能恢复答题
- [ ] 走一遍完整 HR 操作链路

---

## 11. 后续二期可做（不在本次范围）

- Web 管理后台（基于云开发静态托管）
  - HR 可视化创建考试
  - 员工管理界面
  - 成绩在线导出 Excel
  - 题库可视化维护
- 题库 Excel 批量导入工具（参见单独的"题库批量导入方案"）
- 成绩统计与分析（按部门、按时间趋势）
- 错题集中复习模式
- 多场考试并行支持

---

## 12. 待确认事项

- [ ] HR 是否需要"特定部门"参加 / 不参加考试的过滤能力？
- [ ] 多选题打分策略：完全一致才得分 / 漏选给一半 / 自定义？
- [ ] 是否需要"补考"机制（错过当天再开一场）？
- [ ] 成绩页是否对员工显示正确答案 / 解析？
- [ ] 是否要给 HR 一个"撤回考试"或"作废某员工成绩"的入口？

---

## 13. 变更记录

| 日期 | 版本 | 修改 |
|---|---|---|
| 2026-06-27 | v1.0 | 初稿 |
