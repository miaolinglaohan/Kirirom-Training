<view class="page">
  <view class="loading" wx:if="{{loading}}">加载中…</view>

  <block wx:if="{{!loading}}">

    <!-- PDF 水印 -->
    <view class="section">
      <view class="section-title">PDF 水印文字</view>
      <view class="section-desc">
        所有导出的 PDF 都会自动叠加这段半透明斜向水印（45°、灰色 12%、200px 间隔重复）。
        长度上限 60 字符，不支持换行/占位符（{name} 等暂未支持）。
      </view>

      <textarea
        class="input"
        value="{{watermark}}"
        bindinput="onInputWatermark"
        maxlength="60"
        placeholder="例如：基里隆项目部内部资料 · 严禁外传"
        auto-height
      />
      <view class="counter">{{counter}}</view>

      <view class="actions">
        <button class="btn-ghost" size="mini" bindtap="onResetDefault">恢复默认</button>
        <button class="btn-primary" size="mini" loading="{{saving}}" bindtap="onSave">保存</button>
      </view>
    </view>

    <view class="tip">
      ⚠ 仅 admin 可修改本页设置。HR 与普通员工仅在导出 PDF 时读取该值，看不到本页入口。
    </view>
  </block>
</view>
