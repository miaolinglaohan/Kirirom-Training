# 数据库迁移指南

把现有小程序的云开发数据库，迁移到新微信号新建的小程序环境。

> **推荐方案**：云开发控制台导出 JSON + 逐集合导入。全程在浏览器/开发者工具里点鼠标，不装 NodeJS、不配密钥，一次性迁移约 40 分钟。
>
> 备选方案（脚本迁移）见 `scripts/README.md`，适合数据量大或要反复迁移的场景。

## 适用场景

- 用新微信号新建小程序，把现有项目的题库/考试数据迁过去
- 一次性迁移，数据量在几万条以内
- 不想装 NodeJS、不想配腾讯云密钥

## 迁移范围建议

本项目共 **10 个集合**。用户相关数据（`historys` / `notes` / `employees` / `examEnrollments` / `profiles`）里的 `_openid` 是旧微信号的，迁过去对不上人。**建议第一次只迁 4 个题库/配置集合的数据**，另外 6 个只建空集合（不导数据），用户相关的在新小程序里重新积累：

| 集合 | 处理方式 | 说明 |
|---|---|---|
| `exam` | **导出+导入数据** | 题库分类 |
| `subjects` | **导出+导入数据** | 科目 |
| `questions` | **导出+导入数据** | 题目（数据大头） |
| `sysConfig` | **导出+导入数据** | 水印等配置 |
| `employees` | 只建空集合 | 登录时自动建记录 |
| `historys` | 只建空集合 | 答题历史，绑旧 openid 无用 |
| `notes` | 只建空集合 | 收藏，绑旧 openid 无用 |
| `profiles` | 只建空集合 | 用户画像，绑旧 openid 无用 |
| `examEnrollments` | 只建空集合 | 旧答卷，绑旧 openid 无用 |
| `assessments` | 只建空集合 | 旧考试场次已过期，重新建 |

> `assessments` 建议也不迁数据，考试场次绑特定时间段，旧场次迁过去意义不大，新小程序里重新创建考试即可。

### 为什么 6 个不迁数据的集合也要建空的？

云开发集合是**按需访问**，不是启动时全检查。小程序编译、启动都不会因为缺集合报错。但如果用户访问到某个**不存在的集合**对应的功能（比如点「我的收藏」而 `notes` 集合不存在），运行时会报 `DATABASE_COLLECTION_NOT_EXIST`。

为彻底避免任何运行时报错，**这 6 个集合在新环境建空的即可**（不导数据）。建空集合每个 10 秒，6 个约 1 分钟，比导数据快得多。这样所有功能点进去都不会因"集合不存在"报错，只是数据是空的（新用户本来也该是空的）。

> 注：`employees` 集合即供建了空的，新用户首次登录时 `login` 云函数的 `add` 操作也会自动写入记录，不影响功能。

### 两种迁移量对比

| 方案 | 导出+导入数据 | 只建空集合 | 总操作 | 耗时 |
|---|---|---|---|---|
| **推荐：4+6** | 4 个（导出导入各 4 次） | 6 个（只建空） | 14 次点鼠标 | ~15 分钟 |
| 全量 10 个 | 10 个（导出导入各 10 次） | 0 | 20 次点鼠标 | ~25 分钟 |

如果要全量迁移，下面步骤里把"导出/导入数据"的集合数从 4 换成 10 即可，操作完全一样。

---

## 操作步骤

### 第 1 步：旧环境导出集合（5 分钟）

微信开发者工具 -> 顶部「云开发」按钮 -> 数据库 -> 选集合 -> 点右上角「导出」：

1. 选中 `exam` 集合
2. 点「导出」
3. 弹窗里：
   - **格式**：选 **JSON**（⚠ 不要选 CSV，CSV 会把 `options` 数组、`errors` 数组这些嵌套结构拍扁，导入后结构就坏了）
   - **范围**：选「全部记录」
   - **文件名**：默认就行（会下载成 `exam.json`）
4. 点确认，下载到电脑

对要迁移的集合各导一次，文件名就用集合名：
```
exam.json
subjects.json
questions.json
sysConfig.json
```

> **导出格式说明**：云开发导出的 JSON 是 **JSONL 格式**（每行一条记录，不是一个大数组），长得像：
> ```
> {"_id":"xxx","name":"安全规程","options":[...],"_openid":"abc"}
> {"_id":"yyy","name":"消防知识","options":[...],"_openid":"def"}
> ```
> 这种格式控制台导入时能直接识别，不用管。

### 第 2 步：新小程序开通云开发 + 记环境 ID（3 分钟）

1. 用新微信号注册新小程序，登录开发者工具
2. 顶部「云开发」-> 开通（选免费的基础版就行）
3. 控制台首页顶部能看到**环境 ID**（如 `cloud1-yyyyy`），**记下来**

### 第 3 步：新环境手动建全部 10 个空集合（5 分钟）

> ⚠ 这步省不掉。云开发导入时目标集合必须已存在，否则报错；另外 6 个不导数据的集合也要建空的，避免运行时访问报错（见上文「迁移范围建议」说明）。

新环境云开发控制台 -> 数据库 -> 点「+ 新建集合」：

- 输入 `exam`，确认
- 再点「+」，输入下一个，确认
- 把 **10 个集合全建好**：
```
exam  subjects  questions  sysConfig          # 这 4 个要导入数据
employees  historys  notes  profiles          # 这 6 个只建空集合
examEnrollments  assessments                  # 不导数据
```

> 名字必须**一字不差**（区分大小写）。建完后每个集合是空的，0 条记录。
>
> 10 个都建空集合，第 4 步只往其中 4 个（`exam`/`subjects`/`questions`/`sysConfig`）导入数据，其余 6 个保持空即可。

### 第 4 步：逐集合导入（10 分钟）

新环境数据库 -> 选中集合 -> 点「导入」：

1. 选中 `exam` 集合
2. 点「导入」
3. 弹窗里：
   - **文件**：选刚才下载的 `exam.json`
   - **格式**：JSON
   - **冲突处理模式**：选「**插入**」（遇到 _id 冲突时插入新记录，不是覆盖）
4. 点确认，等几秒，提示导入成功，记录数变成和旧环境一样

对 4 个集合（`exam`/`subjects`/`questions`/`sysConfig`）各导入一次，每次选对应的 JSON 文件。其余 6 个集合保持空即可（第 3 步已建好）。

> 导入完成后，建议每个集合点进去看一眼记录数，和旧环境对得上就对了。

### 第 5 步：改小程序代码环境 ID（1 分钟）

**唯一需要改代码的地方**。打开 `miniprogram/app.js`，第 22-24 行：

```js
wx.cloud.init({
  env: 'cloud1-d5gievact76bc75a4',   // 旧环境，改成新环境 ID
  ...
})
```

把 `env` 的值换成新环境 ID（第 2 步记下来的那个），保存。

### 第 6 步：上传部署全部云函数（10 分钟）

> ⚠ 云函数不会随小程序代码自动部署，必须手动上传。

开发者工具左侧目录树 -> `cloudfunctions/` -> 对每个云函数目录右键：

- 右键 `activate` -> 「上传并部署：**云端安装依赖**」（一定要选带"云端安装依赖"的）
- 等上传完成提示
- 对 29 个云函数都做一遍

29 个云函数清单：
```
activate  enterExam  getServerTime  hrDeleteAssessment  hrDeleteEmployee
hrDeleteQuestion  hrDeleteSubject  hrEndAssessment  hrFakeScores
hrGetApplicantReview  hrImportEmployees  hrImportQuestions  hrListAssessmentScores
hrListAssessments  hrListEmployees  hrListExams  hrListQuestions  hrListSubjects
hrSaveAssessment  hrSaveQuestion  hrSaveSubject  hrSetEmployee  hrSysConfig
initDB  listMyAssessments  login  saveDraft  submitExam  whoAmI
```

> 可以批量选中多个右键一起上传吗？**不行**，开发者工具一次只能传一个。建议边传边数，传漏了某个功能会报"云函数不存在"。

### 第 7 步：把自己设成 admin（关键，2 分钟）

> 不做这步，新小程序登录后看不到「管理后台」入口。

1. 开发者工具里用**新微信号**预览/真机调试，登录一次小程序
   - 这会让系统在新环境的 `employees` 集合自动建一条你的记录（普通 role）
2. 回云开发控制台 -> 数据库 -> `employees` 集合
3. 找到**你新微信号**那条记录（看 `name` 字段是你的名字）
4. 双击 `role` 字段，把值从 `"employee"` 改成 `"admin"`
5. 关闭小程序重新打开，首页就出现「🛠 管理后台」入口了

### 第 8 步：调整集合权限规则（5 分钟）

> 新集合默认权限是"仅创建者可读写"，会导致员工端读不到题库。

云开发控制台 -> 数据库 -> 选集合 -> 顶部「**数据权限**」标签 -> 改：

| 集合 | 权限设置 |
|---|---|
| `exam` / `subjects` / `questions` | **所有用户可读**（员工端要读题库） |
| `sysConfig` | **所有用户可读**（导出 PDF 要读水印配置） |
| `employees` / `assessments` / `examEnrollments` | 仅创建者可读写（默认就行，通过云函数访问） |
| `historys` / `notes` / `profiles` | 仅创建者可读写（默认就行） |

> 改完每个集合权限要单独保存。完整权限表见 `docs/部署运维清单.md` 第 56-58 行。

---

## 总耗时

| 步骤 | 耗时 |
|---|---|
| 1. 导出 4 个集合数据 | 5 分钟 |
| 2. 新小程序开通云开发 | 3 分钟 |
| 3. 新环境建 10 个空集合 | 5 分钟 |
| 4. 导入 4 个集合数据 | 10 分钟 |
| 5. 改 app.js 环境 ID | 1 分钟 |
| 6. 上传 29 个云函数 | 10 分钟 |
| 7. 设自己为 admin | 2 分钟 |
| 8. 改集合权限 | 5 分钟 |
| **合计** | **约 40 分钟** |

---

## 常见坑

1. **导出选了 CSV**：题目的 `options` 数组会丢失，导入后题目没选项。**必须选 JSON**。
2. **漏建集合**：导入时报 `DATABASE_COLLECTION_NOT_EXIST`。回去补建该集合再导。
3. **云函数没选"云端安装依赖"**：上传后调用报 `Cannot find module 'wx-server-sdk'`。重新上传时选带"云端安装依赖"的选项。
4. **没设 admin**：登录后看不到「管理后台」。回第 7 步把 `employees` 里自己的 `role` 改成 `admin`。
5. **权限没改**：员工端进不去题库（白屏或报错）。回第 8 步把 `questions`/`subjects`/`exam` 改成"所有用户可读"。
6. **重复导入**：导入操作可重复执行，但会**重复写入**（数据翻倍）。要重导，先到新环境控制台清空对应集合再导。

---

## 验证迁移成功

全部做完后，在新小程序里：

1. 用新微信号登录，首页能看到「顺序练习」「随机刷题」等入口 ✓
2. 进顺序练习，能看到题目（说明 `questions`/`subjects` 迁成功 + 权限对）✓
3. 点「🛠 管理后台」（说明 admin 设置成功）✓
4. 后台「系统设置」里能看到水印文字（说明 `sysConfig` 迁成功）✓
5. 导出一次 PDF，水印正常（说明云函数 `hrSysConfig` 部署成功）✓

这 5 项都通过，迁移就完整了。

---

## 备选方案：脚本迁移

如果数据量大（万条以上）或需要反复迁移，可用 `scripts/` 目录下的 NodeJS 脚本（`@cloudbase/manager`）批量搬数据，详见 `scripts/README.md`。需要本地装 NodeJS + 腾讯云密钥，门槛略高，但跑一次能搬全部 10 个集合。

一次性迁移场景下，**还是本指南的方案 A（控制台导出导入）最省心**。
